__pycache__/
*.py[cod]
*$py.class

# Virtual environments
venv/
env/
.env

# App-specific
uploads/

# Secrets: config thật quản lý qua UI Cài đặt trên VM (bind-mount), KHÔNG bake vào repo.
# Dùng config.example.json làm mẫu; seed config.json thủ công cho môi trường mới.
config.json

# OS specific
.DS_Store
Thumbs.db

# Logs and archives
app.log
*.zip
